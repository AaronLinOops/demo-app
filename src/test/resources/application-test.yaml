---
spring.datasource:
  driverClassName: org.h2.Driver
  url: "jdbc:h2:mem:demo;MODE=MySQL;DATABASE_TO_LOWER=TRUE"
  username: sa
  schema: "classpath:h2_ddl_demo.sql"

spring.h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true